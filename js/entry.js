/*here it is*/
const d3 = require('d3');
const $ = require('jquery');
const Util = require('./Util');

$(document).ready( () => {
(() => {
  "use strict";
  console.log('ready');
  console.log(Util.getCircleRadii);
})()
});
